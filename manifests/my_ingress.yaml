apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: molla-ingress
spec:
  rules:
  - host: molla.info
    http:
      paths:
      - path: /
        pathType: Prefix
        backend:
          service:
            name: molla
            port:
              number: 8000